describe("iFrame",()=>{

    it("test1",()=>{
     //when we have to enter text in iframe where we can not interact with its elements 
        or
     //npm install -D cypress-iframe
     //this commend will install iframe plugin 
     //cy.frameLoaded  , cy.iframe , 
    })
})